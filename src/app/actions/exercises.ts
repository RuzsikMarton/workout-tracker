"use server";

import { CreateExerciseData } from "@/types/exercises";

export async function createExercise(data: CreateExerciseData) {
    // here I will interact with database to create the exercise.
}